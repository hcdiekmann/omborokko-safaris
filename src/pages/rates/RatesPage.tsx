import { Title, Container, Card } from '@mantine/core';

export const RatesPage = (): JSX.Element => {
  return (
    <Container mt={100}>
      <Card>
        <Title order={2}>Our Rates</Title>
      </Card>
    </Container>
  );
};
