import { Center, Title, Container, Loader, Paper, Flex } from '@mantine/core';
import { useContext, useEffect, useState } from 'react';

export const ContactPage = (): JSX.Element => {
  return (
    <Container>
      <Title order={2}>Contact Page</Title>
    </Container>
  );
};
